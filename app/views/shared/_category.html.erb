		<div class="panel panel-default">
		  	<div class="panel-heading">
		    	<h3 class="panel-title">文章分类</h3>
		  	</div>
		  	<div class="panel-body">
				<% @categories.each do |category| %>
					<li> 
						<%= link_to category.name+"("+category.article.length.to_s+")", category %> 
					</li>
				<% end %>
		  	</div>
		</div>